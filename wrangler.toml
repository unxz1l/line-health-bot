name = "line-health-bot"
main = "src/index.js"
compatibility_date = "2024-09-23"

# UTC 00:00 = TST 08:00 (morning meds)
# UTC 12:00 = TST 20:00 (evening meds)
# UTC 14:00 = TST 22:00 (bedtime)
[triggers]
crons = ["0 0 * * *", "0 12 * * *", "0 14 * * *"]

# USER_IDS: set via CLI, not here
#   npx wrangler secret put USER_IDS
# Format: comma-separated LINE user IDs, e.g. "Uxxxx,Uyyyy"

# Secrets (set via CLI, not here):
#   npx wrangler secret put LINE_CHANNEL_SECRET
#   npx wrangler secret put LINE_CHANNEL_ACCESS_TOKEN
#   npx wrangler secret put GROQ_API_KEY
